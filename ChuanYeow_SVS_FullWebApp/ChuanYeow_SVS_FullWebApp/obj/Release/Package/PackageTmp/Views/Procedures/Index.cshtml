@model IEnumerable<ChuanYeow_SVS_FullWebApp.Models.Procedure>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ProcedureID }) |
            @Html.ActionLink("Details", "Details", new { id = item.ProcedureID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ProcedureID })
            @Html.ActionLink("Treatments", "Treatments", new { id = item.ProcedureID })
        </td>
    </tr>
}

</table>
